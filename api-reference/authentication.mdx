---
title: Autenticação
description: Como obter e usar o Token JWT.
icon: 'key'
---

# 🔑 Autenticação (JWT Bearer)

A maioria dos endpoints desta API requer autenticação via **Token Web JSON (JWT)**.

## Obtenção do Token

O token é obtido através do endpoint de login.

### `POST /auth/login`

Autentica um usuário e retorna um JWT para uso em solicitações protegidas.

<Card title="Requisição" icon="terminal">
  ```bash
  curl -X POST /auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "usuario@exemplo.com",
    "password": "sua_senha"
  }'