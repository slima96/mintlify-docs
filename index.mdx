# 🚀 Visão Geral da API

Bem-vindo à documentação da API para a plataforma **Gsafra**. Esta API fornece o *backend* para o Painel de Gerenciamento e integrações críticas, incluindo serviços financeiros e utilitários.

## 🔗 Estrutura das Rotas

A API é composta por três domínios de roteamento principais, definidos nos *routers* do Express:

| Roteador | Prefixo (Base URL) | Descrição Principal |
| :--- | :--- | :--- |
| **`panelRouter`** | `/` (Raiz) | Rotas de gerenciamento de Painel (Usuários, Empresas, Notificações, Treinamentos, Autenticação). |
| **`gsafraRouter`** | `/gsafra` | Rotas específicas de serviços Gsafra (Contas, Papéis, Importação OFX). |
| **`pluggyRouter`** | `/pluggy` | Rotas de integração com a API Pluggy (Tokens, Conectores, Contas, Transações). |

## 🔒 Autenticação

A maioria das rotas está protegida pelo `authMiddleware`. O acesso requer um **Token JWT** válido, que deve ser enviado no cabeçalho `Authorization`.

<a href="/api-reference/authentication">Detalhes sobre a Autenticação</a>

## 🌐 URLs Base

| Ambiente | URL Base |
| :--- | :--- |
| **Produção** | `https://api.gsafra.com` (Exemplo) |
| **Desenvolvimento** | `http://localhost:3003` |